query GetOutlets($params: OutletAdminParams) {
  getAdminOutlets(params: $params) {
    totalCount
    outlets {
      id
      name
      status {
        displayName
      }
      style
      createdOn
      addressBook {
        shippingId
      }
    }
  }
}
